### English Explanation:

**Integration into Existing Code Structure:**
1. **HTML Integration:**
   - Copy the provided HTML code (`index.html`) and paste it into the desired location within your existing HTML file.
   - Ensure that the HTML structure, such as `<head>` and `<body>` tags, is maintained appropriately.

2. **JavaScript Integration:**
   - Save the provided JavaScript code (`BackgroundColourChanger.js`) as a separate JavaScript file in your project directory.
   - Determine where you want the color-changing functionality to be available within your existing JavaScript code.
   - Ensure that the JavaScript file is included in your HTML file using a `<script>` tag, typically just before the closing `</body>` tag.

3. **PHP Integration:**
   - Save the provided PHP code (`BackgroundColourChanger.php`) as a separate PHP file in your project directory.
   - Ensure that the PHP file is in the same directory as your HTML file.
   - If necessary, modify the PHP script to integrate with any existing PHP logic or frameworks in your project.

**Deeper Insights:**
- **HTML Structure:** Ensure that the provided HTML code is integrated within the appropriate sections of your existing HTML file. This may involve adjusting the layout or placement to match your design requirements.
- **JavaScript Integration:** Consider how the color-changing functionality fits into the overall behavior of your web application. You may need to customize the JavaScript code to interact with other components or trigger events based on color changes.
- **PHP Integration:** If your existing PHP codebase utilizes sessions or interacts with databases, ensure that the provided PHP script aligns with your existing setup. You may need to modify variable names or adapt the session management logic to fit your project's architecture.

### Ukrainian Explanation:

**Інтеграція в існуючу структуру коду:**
1. **Інтеграція HTML:**
   - Скопіюйте наданий HTML-код (`index.html`) та вставте його на бажане місце у вашому існуючому HTML-файлі.
   - Переконайтеся, що структура HTML, така як теги `<head>` та `<body>`, зберігається відповідним чином.

2. **Інтеграція JavaScript:**
   - Збережіть наданий JavaScript-код (`BackgroundColourChanger.js`) як окремий файл JavaScript у вашому проектному каталозі.
   - Визначте, куди ви хочете, щоб функціональність зміни кольору була доступною у вашому існуючому JavaScript-коді.
   - Переконайтеся, що файл JavaScript включений у ваш HTML-файл за допомогою тегу `<script>`, зазвичай безпосередньо перед закриваючим тегом `</body>`.

3. **Інтеграція PHP:**
   - Збережіть наданий PHP-код (`BackgroundColourChanger.php`) як окремий файл PHP у вашому проектному каталозі.
   - Переконайтеся, що PHP-файл знаходиться у тій же текі, що і ваш HTML-файл.
   - У разі необхідності відредагуйте PHP-скрипт так, щоб він інтегрувався з існуючою логікою PHP чи фреймворками у вашому проекті.

**Глибші висновки:**
- **Структура HTML:** Переконайтеся, що наданий HTML-код інтегрується у відповідні розділи вашого існуючого HTML-файлу. Це може вимагати адаптації макету чи розташування для відповідності вашим дизайнерським вимогам.
- **Інтеграція JavaScript:** Розгляньте, як функціональність зміни кольору вписується у загальну поведінку вашої веб-застосунку. Вам може знадобитися налаштувати JavaScript-код для взаємодії з іншими компонентами чи спрацьовувати події на основі змін кольору.
- **Інтеграція PHP:** Якщо ваш існуючий PHP-код використовує сесії чи взаємодіє з базами даних, переконайтеся, що наданий PHP-скрипт відповідає вашій поточній конфігурації. Можливо, вам доведеться змінити назви змінних чи адаптувати логіку керування сесіями, щоб відповісти архітектурі вашого проекту.